using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class two : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LogSystem.instance.playEnd2();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
