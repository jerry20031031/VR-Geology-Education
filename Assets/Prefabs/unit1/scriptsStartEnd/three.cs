using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class three : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LogSystem.instance.playEnd22();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
